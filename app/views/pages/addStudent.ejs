<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head") %>
  <body>
    <%- include("../partials/navigation") %> 
    
    <%if (isAdded) {%> <p>The student is added</p> <%}%>
    <%if (!isAdded) {%> <p>    <form action="/students/create" method="POST">
      <div>
        NAME <div><input name="name" type="text" /></div>
      </div>
      <div>
        SURNAME <div><input name="surname" type="text" /></div>
      </div>
      <div>
        AGE <div><input name="age" type="number" /></div>
      </div>
      <div>
        EMAIL <div><input name="email" type="text" />
      </div></div>
       <div>
       Password <div><input name="password" type="password" /></div>
      </div>
      <div>
        GENDER <div><select name="gender">
          <option value="male">male</option>
          <option value="female">female</option>
        </select></div>
      </div>

      <button type="submit">Add Student</button>
    </form></p> <%}%>


  </body>
</html>
